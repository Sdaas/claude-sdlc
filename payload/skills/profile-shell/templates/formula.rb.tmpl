# Homebrew formula for {{NAME}}. Fill in url + sha256 at release time.
class {{CLASS}} < Formula
  desc "{{PURPOSE}}"
  homepage ""
  url ""
  sha256 ""
  license "MIT"

  def install
    bin.install "bin/{{NAME}}"
  end

  test do
    assert_match "Usage", shell_output("#{bin}/{{NAME}} --help")
  end
end
