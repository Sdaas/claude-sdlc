# Build output.
dist/
node_modules/

# Owned by the SDLC scaffold, not the front-end toolchain — leave their
# formatting to the core process, so `test.sh` is green out of the box.
README.md
CLAUDE.md
design/
docs/
